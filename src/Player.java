import java.net.InetAddress;

/**
 * Created by Robin on 11-01-16.
 */
public class Player extends StandaloneClient {
    public Player(InetAddress host, int port) {
        super(host, port);
    }
}
